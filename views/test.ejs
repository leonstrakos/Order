export default function BSRAMysticLanding() { const sections = [ { title: "The Foundation", text: "The Borderline Sciences Research Foundation studies the threshold where forgotten mathematics, celestial anomalies, ritual acoustics, and buried civilizations begin to overlap. Officially, the archives do not exist. Unofficially, they are older than the first library.", }, { title: "Active Domains", text: "Field divisions include Pythagorean harmonics, Sumerian sky tablets, impossible geometries, mnemonic rites, encoded bell frequencies, and cold-case phenomena filed under lunar interference.", }, { title: "Restricted Inquiry", text: "Candidates are admitted only after demonstrating tolerance for paradox, symbolic logic, and silence. The final initiation is said to begin when the bell rings where no bell was ever cast.", }, ];

const dossiers = [ "The Order of the Sun's Bell", "Project Black Tablet", "The Ninth Harmonic Chamber", "Codex of the Hollow Constellation", ];

const symbols = ["✶", "☾", "✦", "⟁", "◬", "✷", "☉", "✺"];

return ( <div className="min-h-screen bg-black text-stone-100 overflow-hidden relative"> <div className="absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(90,90,180,0.18),transparent_35%),radial-gradient(circle_at_bottom,rgba(255,255,255,0.08),transparent_28%)]" /> <div className="absolute inset-0 opacity-30 pointer-events-none"> <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:72px_72px]" /> </div>

<div className="absolute inset-0 pointer-events-none">
    {Array.from({ length: 60 }).map((_, i) => (
      <span
        key={i}
        className="absolute text-[10px] text-indigo-200/60 animate-pulse"
        style={{
          left: `${(i * 37) % 100}%`,
          top: `${(i * 53) % 100}%`,
          animationDelay: `${(i % 7) * 0.4}s`,
        }}
      >
        {symbols[i % symbols.length]}
      </span>
    ))}
  </div>

  <main className="relative z-10 max-w-7xl mx-auto px-6 py-10 md:px-10 lg:px-16">
    <header className="border border-indigo-200/20 rounded-[2rem] bg-white/5 backdrop-blur-sm shadow-2xl shadow-indigo-500/10 overflow-hidden">
      <div className="px-6 py-5 md:px-10 md:py-8 border-b border-indigo-100/10 flex flex-wrap items-center justify-between gap-4">
        <div>
          <p className="text-xs uppercase tracking-[0.45em] text-indigo-200/70">Unauthorized Archive 03</p>
          <p className="text-sm text-stone-300/80 mt-2">Borderline Sciences Research Foundation</p>
        </div>
        <div className="text-right">
          <p className="text-xs uppercase tracking-[0.35em] text-stone-400">Status</p>
          <p className="text-sm text-emerald-300/80">Observing the Threshold</p>
        </div>
      </div>

      <section className="grid lg:grid-cols-[1.2fr_0.8fr] gap-8 px-6 py-10 md:px-10 md:py-14">
        <div className="space-y-6">
          <div className="flex items-center gap-4 text-indigo-100/80 text-sm tracking-[0.3em] uppercase">
            <span>☾</span>
            <span>The Hidden Collegium</span>
            <span>✶</span>
          </div>

          <h1 className="text-5xl md:text-7xl xl:text-8xl leading-[0.95] font-serif tracking-tight text-stone-100 drop-shadow-[0_0_22px_rgba(150,150,255,0.18)]">
            BSRA
            <span className="block text-2xl md:text-4xl xl:text-5xl mt-5 text-indigo-100/90">
              Borderline Sciences Research Foundation
            </span>
          </h1>

          <p className="max-w-2xl text-base md:text-lg leading-8 text-stone-300/85">
            A clandestine institution for the study of mystical evidence, fringe cosmology,
            sacred number systems, anomalous bells, vanished empires, and the disciplined
            crossing point between science and forbidden memory.
          </p>

          <div className="flex flex-wrap gap-4 pt-2">
            <button className="rounded-full border border-indigo-200/30 bg-indigo-200/10 px-6 py-3 text-sm uppercase tracking-[0.25em] text-indigo-50 shadow-lg shadow-indigo-500/10 hover:bg-indigo-200/15 transition">
              Request Admission
            </button>
            <button className="rounded-full border border-stone-200/20 px-6 py-3 text-sm uppercase tracking-[0.25em] text-stone-200/80 hover:bg-white/5 transition">
              View Dossiers
            </button>
          </div>
        </div>

        <div className="relative">
          <div className="absolute -inset-4 rounded-[2rem] bg-indigo-300/10 blur-3xl" />
          <div className="relative rounded-[2rem] border border-indigo-100/20 bg-black/50 p-6 md:p-8 h-full shadow-2xl">
            <div className="flex items-center justify-between mb-8">
              <span className="text-xs uppercase tracking-[0.4em] text-indigo-200/70">Primary Seal</span>
              <span className="text-stone-500">◌</span>
            </div>

            <div className="aspect-square rounded-full border border-indigo-100/15 flex items-center justify-center relative bg-[radial-gradient(circle,rgba(95,95,220,0.18),rgba(0,0,0,0.1)_35%,transparent_60%)]">
              <div className="absolute inset-6 rounded-full border border-dashed border-indigo-100/20" />
              <div className="absolute inset-14 rounded-full border border-indigo-100/15" />
              <div className="absolute inset-20 rounded-full border border-dashed border-stone-100/10" />
              <div className="text-center px-8">
                <div className="text-5xl md:text-6xl mb-3">☉</div>
                <div className="text-lg uppercase tracking-[0.45em] text-indigo-100/85">Sun Bell</div>
                <div className="mt-4 text-sm leading-6 text-stone-300/75">
                  Resonant key for sealed archives and celestial rites.
                </div>
              </div>
            </div>

            <div className="grid grid-cols-3 gap-3 mt-8 text-center text-xs uppercase tracking-[0.3em] text-stone-400">
              <div className="rounded-2xl border border-white/10 py-3">Sky</div>
              <div className="rounded-2xl border border-white/10 py-3">Number</div>
              <div className="rounded-2xl border border-white/10 py-3">Ritual</div>
            </div>
          </div>
        </div>
      </section>
    </header>

    <section className="grid lg:grid-cols-[0.9fr_1.1fr] gap-6 mt-10">
      <div className="rounded-[2rem] border border-white/10 bg-white/5 backdrop-blur-sm p-6 md:p-8 shadow-xl">
        <p className="text-xs uppercase tracking-[0.45em] text-indigo-200/70">Foundational Statement</p>
        <h2 className="text-3xl md:text-4xl font-serif mt-5 text-stone-100">
          Not occult. Not academic. Both.
        </h2>
        <p className="mt-5 text-stone-300/85 leading-8">
          BSRA operates in the interval between classified research and sacred tradition.
          Its members collect impossible correspondences: stars echoing old temple ratios,
          bells reproducing encoded maps, and witnesses describing the same geometry across
          centuries of unrelated civilizations.
        </p>

        <div className="mt-8 rounded-[1.5rem] border border-indigo-100/15 bg-black/40 p-5">
          <p className="text-xs uppercase tracking-[0.35em] text-stone-500 mb-4">Recovered Motto</p>
          <p className="font-serif text-2xl text-indigo-50/95 leading-relaxed">
            “Where reason thins, the pattern begins.”
          </p>
        </div>
      </div>

      <div className="grid md:grid-cols-3 gap-6">
        {sections.map((section) => (
          <article
            key={section.title}
            className="rounded-[2rem] border border-white/10 bg-gradient-to-b from-white/8 to-white/4 p-6 shadow-xl"
          >
            <div className="text-indigo-200/80 text-2xl mb-4">✦</div>
            <h3 className="text-2xl font-serif text-stone-100">{section.title}</h3>
            <p className="mt-4 text-sm leading-7 text-stone-300/80">{section.text}</p>
          </article>
        ))}
      </div>
    </section>

    <section className="grid lg:grid-cols-[1fr_0.9fr] gap-6 mt-10">
      <div className="rounded-[2rem] border border-white/10 bg-white/5 p-6 md:p-8 shadow-xl">
        <div className="flex items-center justify-between gap-4 flex-wrap">
          <div>
            <p className="text-xs uppercase tracking-[0.45em] text-indigo-200/70">Current Dossiers</p>
            <h2 className="text-3xl font-serif mt-4">Restricted Cases</h2>
          </div>
          <div className="rounded-full border border-indigo-100/15 px-4 py-2 text-xs uppercase tracking-[0.35em] text-stone-400">
            Level VII Access
          </div>
        </div>

        <div className="mt-8 space-y-4">
          {dossiers.map((item, idx) => (
            <div
              key={item}
              className="group rounded-[1.5rem] border border-white/10 bg-black/35 px-5 py-4 flex items-center justify-between hover:border-indigo-200/30 hover:bg-white/5 transition"
            >
              <div>
                <p className="text-xs uppercase tracking-[0.35em] text-stone-500">File {String(idx + 1).padStart(2, "0")}</p>
                <p className="mt-2 text-lg font-serif text-stone-100 group-hover:text-indigo-100 transition">{item}</p>
              </div>
              <div className="text-indigo-200/70 text-xl">⟶</div>
            </div>
          ))}
        </div>
      </div>

      <div className="rounded-[2rem] border border-white/10 bg-[linear-gradient(180deg,rgba(255,255,255,0.06),rgba(255,255,255,0.02))] p-6 md:p-8 shadow-xl">
        <p className="text-xs uppercase tracking-[0.45em] text-indigo-200/70">Transmission</p>
        <h2 className="text-3xl font-serif mt-4 text-stone-100">Entrance Protocol</h2>
        <ol className="mt-6 space-y-5 text-stone-300/85">
          {[
            "Observe the sky at the hour of second silence.",
            "Memorize the ratio hidden in the bell sequence.",
            "Enter only with a name you have never spoken aloud.",
            "Bring no certainty past the threshold.",
          ].map((step, i) => (
            <li key={step} className="flex gap-4">
              <span className="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-indigo-100/20 text-xs text-indigo-100">
                {i + 1}
              </span>
              <span className="leading-7">{step}</span>
            </li>
          ))}
        </ol>

        <div className="mt-8 rounded-[1.5rem] border border-indigo-100/15 bg-black/40 p-5">
          <p className="text-xs uppercase tracking-[0.35em] text-stone-500">Field Note</p>
          <p className="mt-3 text-sm leading-7 text-stone-300/80">
            Internal references connect Pythagorean interval theory with Sumerian astronomical
            records and modern anomaly reports formerly dismissed as folklore, hoax, or stress-induced patterning.
          </p>
        </div>
      </div>
    </section>

    <footer className="mt-10 rounded-[2rem] border border-white/10 bg-white/5 px-6 py-6 md:px-8 md:py-7 flex flex-col md:flex-row gap-4 md:items-center md:justify-between text-sm text-stone-400">
      <div>
        <span className="uppercase tracking-[0.35em] text-indigo-200/70">BSRA</span>
        <span className="mx-3 text-stone-600">/</span>
        Borderline Sciences Research Foundation
      </div>
      <div className="uppercase tracking-[0.28em] text-xs">
        Mystical Research · Forbidden Archives · Celestial Evidence
      </div>
    </footer>
  </main>
</div>

); }
